use_default_settings: true
server:
  bind_address: "0.0.0.0"
  port: 8080
  # Set via SEARXNG_SECRET_KEY environment variable in docker-compose.yml
  secret_key: "changeme"
  public_instance: false
search:
  formats:
    - json
  max_page: 5
  safesearch: 0
  default_lang: "en"
